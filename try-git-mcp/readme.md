Explore how to use 'git-mcp'
* website: https://github.com/idosal/git-mcp
* repo which I want to use git-mcp: https://github.com/feature-engine/feature_engine

If any packages need to be installed,
* please create and use a docker container, and install the packages in the docker container
* this is because I do not want to install packages for this project that conflict with other packages on my local machine
