{
    "name": "coding-interview-app",
    "version": "1.0.0",
    "description": "End-to-end coding interview application",
    "scripts": {
        "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
        "dev:backend": "docker-compose -f docker-compose.dev.yml exec dev uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload",
        "dev:frontend": "docker-compose -f docker-compose.dev.yml exec dev sh -c 'cd frontend && npm run dev'",
        "docker:up": "docker-compose -f docker-compose.dev.yml up -d",
        "docker:down": "docker-compose -f docker-compose.dev.yml down",
        "docker:build": "docker-compose -f docker-compose.dev.yml build"
    },
    "devDependencies": {
        "concurrently": "^9.1.0"
    }
}